global:
  scrape_interval: 15s
scrape_configs:
  - job_name: retrieval
    static_configs: [{targets: ["retrieval:8100"]}]
    metrics_path: /metrics
  - job_name: workers
    static_configs: [{targets: ["worker:9000"]}]
